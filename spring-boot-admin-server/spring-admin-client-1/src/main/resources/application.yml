server:
  port: 8081
spring:
  boot:
    admin:
      client:
        url:
        - http://localhost:8080
  application:
    name: Client 1
    
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always